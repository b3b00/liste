User must have a way to access all the lists he has already working on. This is to say that in settings the list text field should be a autocomplete field instead of a raw text field.
the lists shoudl be saved in local storage along their version. 
Save lists and categories together to ensure there is no inconsistency