#goal

rework sharing.
if and only online when receiving a share link , check if list is available on back end and load it from there. this will allow auto sync => the list is then not shared in the inbox but as a classic list. if offline keep current behavior : store locally in inbox.